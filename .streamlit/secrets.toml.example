# Copy this file to secrets.toml and replace the placeholder with your key.
# Never commit secrets.toml. On Streamlit Community Cloud, set
# ANTHROPIC_API_KEY in the app's Secrets settings instead.
ANTHROPIC_API_KEY = "your-anthropic-api-key"
